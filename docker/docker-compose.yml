version: "3"
services:
    tails-server:
        build:
            context: ..
            dockerfile: docker/Dockerfile
        ports:
            - 6543:6543
        command: >
            tails-server --host 0.0.0.0 --port 6543