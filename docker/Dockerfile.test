FROM python:3.13

ADD pyproject.toml .
ADD README.md .
ADD tails_server ./tails_server

# Use Python 3.13's pip to install dependencies
RUN python3.13 -m pip install --no-cache-dir .[dev]

ADD test ./

ENTRYPOINT ["python3.13", "integration.py"]
